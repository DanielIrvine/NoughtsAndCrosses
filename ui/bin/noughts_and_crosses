#! /usr/bin/env ruby

$LOAD_PATH.unshift File.dirname(__FILE__) + '/../lib'

require 'rubygems'
require 'bundler/setup'
require 'cli_display'

module NoughtsAndCrosses
  module CLI
    display = CLIDisplay.new(Kernel)
    display.begin
    display.exec
  end
end

