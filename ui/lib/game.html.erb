<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <style type="text/css">
      <!--
      body { text-align: center; font-family: Helvetica; }
      table td { border: 1px solid black;  }
      table { width: 600px; height: 600px; table-layout: fixed; margin: auto; }
      td { vertical-align: middle; font-size: 30pt; }
      td a { display: block;  width: 100%; height: 100%;} 
      .status { 
        font-size: 20pt; 
      }
      -->
    </style>
    <% if !next_move.nil? %>
      <meta http-equiv="refresh" content="1; URL=<%= next_move %>" />
    <% end %>
  </head>

  <body>
    <table>
      <% rows.each do |row| %>
        <tr>
          <% row.each do |sq| %>
            <td>
                <% if !sq.nil? %>
                  <% if !sq[:link].nil? %>
                    <a href="<%= sq[:link] %>"> </a>
                  <% else %>
                      <%= sq[:text] %>     
                  <% end %>
                <% end %>
            </td>
          <% end %>
        </tr>
      <% end %>
    </table>
    <% if next_turn %><p class="status"><%= next_turn %></p><% end %>
    <% if result %>
      <p class="status"><%= result %></p>
      <p class="status"><a href="/"><%= translate(:play_again_link) %></a></p>
    <% end %>
  </body>
</html>

