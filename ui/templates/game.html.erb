<!DOCTYPE html> 
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="/public/styles.css">
    <% if !next_move.nil? %>
      <meta http-equiv="refresh" content="1; URL=<%= next_move %>" />
    <% end %>
  </head>

  <body>
    <table>
      <% rows.each do |row| %>
        <tr>
          <% row.each do |sq| %>
            <td>
                <% if !sq.nil? %>
                  <% if !sq[:link].nil? %>
                    <a href="<%= sq[:link] %>"> </a>
                  <% else %>
                      <%= sq[:text] %>
                  <% end %>
                <% end %>
            </td>
          <% end %>
        </tr>
      <% end %>
    </table>
    <% if next_turn %><p class="status"><%= next_turn %></p><% end %>
    <% if result %>
      <p class="status"><%= result %></p>
      <p class="status"><a href="/"><%= translate(:play_again_link) %></a></p>
    <% end %>
  </body>
</html>


