#! /usr/bin/env ruby

$LOAD_PATH.unshift File.dirname(__FILE__) + '/../lib'

require 'game_builder'
require 'cli_display'

game = GameBuilder.new(CLIDisplay.new(Kernel)).build
game.play_all! until game.board.game_over?
